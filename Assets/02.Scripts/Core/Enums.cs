

public enum AudioType { Master, BGM, SFX }

public enum AudioID
{
    PistolFire
}


public enum ItemType
{
    Weapon,
    Consumable,
    Collect
}


public enum QuestType
{
    Kill,
    Collect,
    Reach
}