

public enum AudioType { Master, BGM, SFX }

public enum AudioID
{
    PistolFire,
    PlayerWalk1,
    PlayerWalk2
}


public enum ItemType
{
    Weapon,
    Consumable,
    Collect
}


public enum StageObjectiveType
{
    Kill,
    Collect,
    Reach
}